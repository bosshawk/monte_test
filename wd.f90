    Subroutine WD_renzu(xb,yb,zb,x,y,z)
    implicit none
    
     real (kind(0D0)) xb,yb,zb,x,y,z
 real (kind(0Q0)) xb1,yb1,zb1,x1,y1,z1
 
 print*,x
 x1=x*1q0
 print*,x1
 
 !dx = 1.0;
 !dy = 3.0;
 !dsqrt2 = sqrt(2.0)
 !dpi = 4.0D0*atan(1.0)
 !qx = 1.0;
 !qy = 3.0;
 !qsqrt2 = sqrt(2.0)
 !qpi = 4.0*atan(1.0)
 !print *, "---- double precision ----"
 !print *, "1/3     =", dx/dy
 !print *, "pi      =", dpi
 !print *, "sqrt(2) =", dsqrt2
 !print *, "----  quad  precision ----"
 !print *, "1/3     =", qx/qy
 !print *, "pi      =", qpi
 !print *, "sqrt(2) =", qsqrt2
    
    end